{% extends 'security/_layout.html.twig' %}

{% block title %}
    {{ 'page.create_password.title'|trans }}
{% endblock %}

{% block content %}
    <div class="max-w-xs w-full flex flex-col items-center justify-center gap-4 lg:gap-6">
        <twig:Badge icon="lock" class="w-16" />
        <div class="flex flex-col gap-1">
            <h1><twig:Typography variant="heading-1" bold center>{{ 'page.create_password.title'|trans }}</twig:Typography></h1>
            <p><twig:Typography variant="body-lg" center>{{ 'page.create_password.description'|trans }}</twig:Typography></p>
        </div>
        {{ form_start(form, {'attr': {'class': 'w-full flex flex-col gap-4'}}) }}
        {{ form_row(form.password, {'attr': {'class': 'w-full flex flex-col gap-4 lg:gap-5'}}) }}
        {{ form_end(form) }}
    </div>
{% endblock %}

