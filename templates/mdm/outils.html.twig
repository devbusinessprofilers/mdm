{#
    Maquette : « MDM prototype.dc.html », pages `journal` / `outils` et `dam`.

    Deux écrans sous un même rail : le journal des traitements et la
    bibliothèque de médias. Même châssis — en-tête, bandeau d'indicateurs,
    tableau, pied — seule la matière change.

    Les trois autres outils du rail n'ont pas de gabarit : ils mènent à l'écran
    d'attente, comme partout ailleurs.
#}
{% extends 'mdm/base.html.twig' %}

{% set ecran = 'outils' %}

{% set grille = 'grid gap-4 items-center px-4 grid-cols-[repeat(auto-fit,minmax(0,1fr))]' %}

{% block title %}{{ vue.titre }}{% endblock %}

{% block rail %}
    <nav class="flex flex-col shrink-0 box-border w-[328px] pl-5 pr-3 overflow-hidden
                {{ rail_pad_y }} {{ rail_group_gap }}"
         aria-label="Outils">

        <div class="flex flex-col {{ rail_title_gap }}">
            <twig:Typography variant="body" bold
                             class="pl-2 pr-4 [@media(max-height:700px)]:leading-[1.125rem]!">Outils</twig:Typography>
            <div class="flex flex-col">
                {% for entree in vue.entrees %}
                    <a href="{{ entree.integre
                              ? path('app_mdm_outils')
                              : path('app_mdm_a_venir', { ecran: entree.cle }) }}"
                       class="group/outil flex items-center gap-2 box-border px-4 {{ rail_item_pad_y }}
                              rounded-xs overflow-hidden cursor-pointer
                              hover:bg-primary-4 aria-[current=page]:bg-linear-(--primary-gradient)"
                       {{ entree.actif ? 'aria-current="page"' : '' }}>
                        <twig:Icon name="{{ entree.icone }}" aria-hidden="true"
                                   class="shrink-0 self-center w-4 h-4 mx-1 text-primary
                                          group-aria-[current=page]/outil:text-neutral-100" />
                        <twig:Typography variant="body" bold ellipsis textColor="primary-3"
                                         class="flex-1 min-w-0
                                                group-aria-[current=page]/outil:text-neutral-100!">{{ entree.libelle }}</twig:Typography>
                        {% if entree.badge %}
                            <twig:Typography variant="body-xs" bold textColor="neutral-500"
                                             class="shrink-0 px-[7px] py-0.5 rounded-lg bg-neutral-200
                                                    group-aria-[current=page]/outil:bg-neutral-100
                                                    group-aria-[current=page]/outil:text-primary-3!">{{ entree.badge }}</twig:Typography>
                        {% endif %}
                    </a>
                {% endfor %}
            </div>
        </div>
    </nav>
{% endblock %}

{% block contenu %}
    <div class="shrink-0 flex flex-col gap-1 px-10 pt-[22px] pb-4">
        <twig:Typography variant="heading-2" element="h1"
                         class="sm:text-[1.625rem]! sm:leading-[2.125rem]! font-[700]!">{{ vue.titre }}</twig:Typography>
        <twig:Typography variant="body" element="p" textColor="neutral-500"
                         class="text-pretty">{{ vue.sousTitre }}</twig:Typography>
    </div>

    <div class="flex-1 min-h-0 flex flex-col gap-4 px-10 pb-8 overflow-y-auto scrollbar">

        <div class="shrink-0 flex flex-row gap-3.5 flex-wrap">
            {% for indicateur in vue.indicateurs %}
                <div class="flex flex-col flex-1 basis-48 min-w-0 box-border gap-1.5 px-5 py-4
                            rounded-sm bg-neutral-100 shadow-(--shadow)">
                    <div class="flex items-center gap-2">
                        {# Le fond de la pastille dit la nature du compteur, pas sa valeur. #}
                        <span class="flex items-center justify-center shrink-0 w-7 h-7 rounded-lg
                                     {{ indicateur.fond }}">
                            <twig:Icon name="{{ indicateur.icone }}" aria-hidden="true"
                                       class="w-4 h-4 text-primary-3" />
                        </span>
                        <twig:Typography variant="body-xs" bold textColor="neutral-500" ellipsis
                                         class="min-w-0 tracking-[0.3px] uppercase">{{ indicateur.libelle }}</twig:Typography>
                    </div>
                    <twig:Typography variant="subtitle" class="font-[700]!">{{ indicateur.valeur }}</twig:Typography>
                    <twig:Typography variant="body-sm" textColor="neutral-500">{{ indicateur.note }}</twig:Typography>
                </div>
            {% endfor %}
        </div>

        {#
            Colonnes en fractions et non en pixels : le handoff dimensionne pour
            un cadre de 1920 avec un rail de 284, celui-ci fait 328.
        #}
        <div class="shrink-0 flex flex-col rounded-sm overflow-hidden bg-neutral-100 shadow-(--shadow)">

            <div class="{{ grille }} h-9 shrink-0 bg-primary-5 border-b border-neutral-200" role="row">
                {% for colonne in vue.colonnes %}
                    <span class="min-w-0 truncate {{ colonne.aligne }}" role="columnheader">
                        <twig:Typography variant="body-xs" bold textColor="neutral-500"
                                         class="tracking-[0.3px] uppercase">{{ colonne.libelle }}</twig:Typography>
                    </span>
                {% endfor %}
            </div>

            <div class="flex flex-col">
                {% for ligne in vue.lignes %}
                    <button type="button" role="row"
                            class="{{ grille }} w-full min-h-[56px] py-2 box-border text-left cursor-pointer
                                   hover:bg-primary-5 {{ loop.first ? '' : 'border-t border-neutral-200' }}">
                        {% for cellule in ligne.cellules %}
                            {{ _self.cellule(cellule) }}
                        {% endfor %}
                    </button>
                {% endfor %}
            </div>

            <div class="shrink-0 flex items-center h-[50px] px-4 border-t border-neutral-200">
                <twig:Typography variant="body-sm" textColor="neutral-500">{{ vue.pied }}</twig:Typography>
            </div>
        </div>
    </div>
{% endblock %}


{% macro cellule(cellule) %}
    {% if 'jeton' == cellule.type %}
        <span class="min-w-0">
            <span class="inline-flex w-fit max-w-full px-[9px] py-[3px] rounded-lg {{ cellule.fond }}">
                <twig:Typography variant="body-xs" bold ellipsis
                                 textColor="{{ cellule.teinte }}">{{ cellule.texte }}</twig:Typography>
            </span>
        </span>
    {% elseif 'duo' == cellule.type %}
        <span class="flex flex-col min-w-0">
            <twig:Typography variant="body-sm" bold ellipsis>{{ cellule.texte }}</twig:Typography>
            <twig:Typography variant="body-xs" textColor="neutral-400" ellipsis>{{ cellule.note }}</twig:Typography>
        </span>
    {% elseif 'lien' == cellule.type %}
        <span class="flex items-center justify-end gap-1.5 min-w-0">
            <twig:Typography variant="body-sm" bold textColor="primary-3">{{ cellule.texte }}</twig:Typography>
            <twig:Icon name="arrow-right" aria-hidden="true" class="shrink-0 w-3 h-3 text-primary" />
        </span>
    {% else %}
        <span class="min-w-0 {{ cellule.aligne }}">
            <twig:Typography variant="body-sm" bold="{{ cellule.gras }}" ellipsis
                             textColor="{{ cellule.discret ? 'neutral-500' : 'neutral-900' }}">{{ cellule.texte }}</twig:Typography>
        </span>
    {% endif %}
{% endmacro %}
