{% block activity_option_row %}
    <div class="w-full grid grid-cols-1 lg:grid-cols-3 gap-x-6 gap-y-4">
        {{ form_row(form.name) }}
        {{ form_row(form.capacity) }}
        {{ form_row(form.price) }}
    </div>
{% endblock %}
