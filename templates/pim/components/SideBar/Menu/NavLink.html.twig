<twig:Button
    asLink
    variant="navigation"
    href="{{ path(route, routeParameters) }}"
    icon="{{ icon }}"
    iconColor="{{ iconColor.value }}"
    isActive="{{ isActive }}"
    size="{{ size.value }}"
    full
>
    {{ label }}
</twig:Button>
