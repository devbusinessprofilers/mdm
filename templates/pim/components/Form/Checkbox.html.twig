{% set content %}{% block content %}{% endblock %}{% endset %}
{% import 'pim/components/Form/_checkbox_markup.html.twig' as form_parts %}

{# Le markup vit dans _checkbox_markup.html.twig, partagé avec la boucle
   d'options de Form:Select pour éviter d'instancier un composant par option. #}
{{ form_parts.checkbox(id, name, value, checked, disabled, inputAttributes, attributes.render('class'), content) }}
