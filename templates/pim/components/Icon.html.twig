{# La logique de préfixe vit dans la macro _icon_markup.html.twig, partagée
   avec les points chauds qui rendent l'icône sans instancier le composant. #}
{% import 'pim/components/_icon_markup.html.twig' as icon_parts %}
{{ icon_parts.icon(name, attributes.all()) }}
