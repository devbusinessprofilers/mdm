<div{{ attributes }}>
    <a href="{{ path('provider_portal_index') }}" title="Tableau de bord" class="block w-full focus:outline-none">
        <img class="w-full" src="{{ asset('/img/logo.svg') }}" alt="Logo" />
    </a>
</div>

