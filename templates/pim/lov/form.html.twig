{% extends 'mdm/simple.html.twig' %}
{% block title %}{{ creation ? 'Ajouter une valeur' : 'Modifier ' ~ value.code }}{% endblock %}
{% block corps %}<main class="page page-narrow"><a class="back-link" href="{{ path('app_pim_lov_show', {code: attribute.code}) }}">← Retour à {{ attribute.label }}</a><header class="page-header"><div><p class="page-kicker"><code>{{ attribute.code }}</code></p><h1>{{ creation ? 'Ajouter une valeur' : 'Modifier la valeur' }}</h1></div></header><section class="card">{{ form(form) }}</section></main>{% endblock %}
