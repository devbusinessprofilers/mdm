<h2><twig:Typography variant="heading-2" bold class="mt-8 mb-4 block">Score</twig:Typography></h2>
<div class="flex gap-8 flex-col bg-neutral-100 rounded-sm p-8  shadow-(--shadow)">
    <ul class="flex max-w-full gap-4">
        <li>
            <twig:Score value="{{ 5 }}" />
        </li>
        <li>
            <twig:Score value="{{ 4.95 }}" />
        </li>
    </ul>
</div>
