<h2><twig:Typography variant="heading-2" bold class="mt-8 mb-4 block">Pagination</twig:Typography></h2>
<div class="flex gap-8 flex-col bg-neutral-100 rounded-sm p-8 shadow-(--shadow)">
    <ul class="flex flex-col max-w-full gap-4">
        <li><twig:Pagination total="{{ 16 }}" current="{{ 10 }}" /></li>
        <li><twig:Pagination total="{{ 16 }}" current="{{ 1 }}" /></li>
        <li><twig:Pagination total="{{ 16 }}" current="{{ 2 }}" /></li>
        <li><twig:Pagination total="{{ 16 }}" current="{{ 15 }}" /></li>
        <li><twig:Pagination total="{{ 16 }}" current="{{ 16 }}" /></li>
        <li><twig:Pagination total="{{ 7 }}" current="{{ 1 }}" /></li>
        <li><twig:Pagination total="{{ 7 }}" current="{{ 4 }}" /></li>
        <li><twig:Pagination total="{{ 7 }}" current="{{ 7 }}" /></li>
        <li><twig:Pagination total="{{ 3 }}" current="{{ 2 }}" /></li>
        <li><twig:Pagination total="{{ 1 }}" current="{{ 1 }}" /></li>
    </ul>
</div>
