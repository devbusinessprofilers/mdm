{% extends 'pim/pages/sheet/_layout.html.twig' %}

{% block title %}
    {{ 'page.sheet.place.csr.title'|trans }}
{% endblock %}

{% block main %}
    {{ form_start(form, { 'attr': { 'id': form.vars.id, 'class': 'flex flex-col gap-6 lg:gap-8'} }) }}
    <section class="flex flex-col gap-6 bg-neutral-100 rounded-md shadow-(--shadow) w-full p-6 2xl:p-14">
        <h2><twig:Typography variant="heading-3" bold>{{ 'page.sheet.place.csr.title'|trans }}</twig:Typography></h2>
        {{ form_row(form.commitmentFile, {'attr': {'class': 'lg:col-span-2'}}) }}
        <div class="w-full grid grid-cols-1 lg:grid-cols-2 gap-x-6 gap-y-4">
            {{ form_row(form.purchase) }}
            {{ form_row(form.environmentalImpact) }}
            {{ form_row(form.socialImpact) }}
            {{ form_row(form.purchaseCategory) }}
            {{ form_row(form.mobility) }}
            {{ form_row(form.distinction) }}
        </div>
    </section>
    {{ form_end(form) }}
{% endblock %}

{% block float_submit %}
    <twig:Form:FloatSubmit label="{{ 'global.save'|trans }}" formId="{{ form.vars.id }}" />
{% endblock %}
